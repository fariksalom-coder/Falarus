# Yagona UI/UX — amaldagi ilovaga integratsiya

Maqsad: umumiy UI tizimini amaldagi ilovaga ulash va 82.115.50.100 serveriga deploy qilish.

## O'zgarishlar

- `app` va `pmn` neytral palitralari bir manbaga bog‘landi. Auth, profil, statistika va premium sahifalar sarlavhalari yagona sans-serif oilasiga o‘tdi. Tungi success rangi tuzatildi.
- Desktopda 224px chap navigatsiya; telefonda amaldagi 6/7 bo‘limli pastki menyu. Mention belgisi, role ruxsatlari, route prefetch va mashq/to‘lovdagi fokus rejimi saqlandi. Reduced-motion hisobga olindi.
- O‘yin sahifasi takroriy gradient kartalaridan umumiy Card, Badge, Button va Progress komponentlariga o‘tdi. O‘yinlar manzillari, bepul quota, login yo‘naltirish va progress API ishlashi saqlandi.
- Kurslar, profil, statistika uchun umumiy PageHeader; auth tugmalari umumiy Button; admin kirishi umumiy Card/Field/Button asosida. Auth label va xatolar inputga bog‘landi.
- Ustoz kabinetining umumiy komponentlari va bo‘limlaridagi neytral/brand ranglari semantik tokenlarga o‘tkazildi.
- Admin sahifalaridagi sirt va matnlar umumiy palitraga o‘tdi. Sidebar telefonda tugma orqali ochiladi, route tanlanganda yopiladi; desktopda doim ko‘rinadi. Asosiy menyu nomlari o‘zbekchalashtirildi.
- PWA kesh versiyasi v24.

## Chegara

Bu amaldagi komponent va sahifalarga qo‘llangan UI integratsiyasi. 77 sahifaning barcha ichki jarayoni boshqatdan yozilmadi; dars mexanikasi, to‘lov, ruxsatlar, ma’lumotlar bazasi o‘zgartirilmadi. Alohida interaktiv prototip ishlab chiqarishdagi auth marshrutlariga qo‘shilmadi.

## Tekshirish va zaxira

Mahalliy TypeScript, test va build bajarildi. O‘yin/kurs/profil/statistika/tarif/kirish/admin/ustoz sahifalari mahalliy API fixture bilan brauzerda tekshiriladi; bu haqiqiy to‘lov yoki haqiqiy ustoz band qilish testi emas. Tashqi Google skripti test brauzerida ataylab bloklanadi.

Deploy oldi manba zaxirasi: `/home/ubuntu/backups/ui-before-20260907.tar.gz`.
Fayl nusxalari: `/home/ubuntu/backups/ui-files-20260907`.
Build almashtirish: `scripts/deploy-build.sh`, oldingi yig‘ma `dist.rollback` da saqlanadi.

## Yakuniy tekshiruv natijalari

- Server TypeScript tekshiruvi va 185 test: muvaffaqiyatli.
- 8 amaldagi sahifa × 375/390/1440px: 24 ko‘rinishda sahifa overflow yoki error boundary bo‘lmadi. Fixture testi dastlab Google SDK bloklanishidagi unhandled rejectionni aniqladi; `useGoogleSignIn` initializatsiya xatosi va unmount holati tuzatildi.
- Keyingi yangi brauzer sessiyasi: 390/1440px da 8 ko‘rinish, 3 interaksiya (parolni ko‘rsatish va admin mobil menyusi), 0 Runtime exception. [Yakuniy natija](live-final-check.json). Tungi skrinshotlarda telefon maydoni, tab va parol ikonka kontrasti tekshirildi.
- Deploydan avvalgi build alohida `/home/ubuntu/backups/ui-dist-before-20260907` da saqlandi; `dist.rollback` eng so‘nggi almashtirishdan oldingi versiya.
- Build muvaffaqiyatli, avvalgi katta o‘yin chunk ogohlantirishi saqlanadi.

Ommaviy tekshiruv: HTML 200, CSS 200, `/api/health` ok, PWA v24; yangi navigatsiya va telefon maydoni tokenlari chiqarilgan CSS ichida mavjud. Yakuniy rang tokenlari mavjud CSS deklaratsiyalari bilan solishtirildi; noto‘g‘ri gradient/hover nomlari tuzatildi.

**Deploy yakunlandi.** `https://falarus.uz/login` ommaviy brauzerda ochildi: sarlavha “Kirish”, gorizontal overflow yo‘q, error boundary yo‘q, brend gradienti hisoblangan CSS orqali tasdiqlandi. Yakuniy rsync checksum dry-run hech qanday farq qaytarmadi. Serverdagi 59 faylli UI manifesti mahalliy nusxaga mos.
